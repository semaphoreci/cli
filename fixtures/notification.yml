apiVersion: v1alpha
kind: Notification
metadata:
  name: test
spec:
  rules:
    - name: "Rule #1"
      filter:
        projects:
          - cli
        branches:
          - master
        tags:
          - v1.0.0
          - v2.*
        pipelines:
          - semaphore.yml
      notify:
        slack:
          endpoint: "https://hooks.slack.com/asdasdasd/sada/sdas/da"

