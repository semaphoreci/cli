version: "v1.0"
name: Edge
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2404

blocks:
  - name: "Edge Release"
    task:
      secrets:
        - name: container-registry-writer

      jobs:
      - name: "Release"
        commands:
          - checkout
          - make gsutil.configure
          - make release.edge.install.script
